diff --git a/cosima_core/simulators/communication_simulator.py b/cosima_core/simulators/communication_simulator.py
index 3333333..4444444 100644
--- a/cosima_core/simulators/communication_simulator.py
+++ b/cosima_core/simulators/communication_simulator.py
@@ -1,6 +1,7 @@
 import socket
 import threading
 import time
+import os
 
 # ... (resto dos imports existentes)
 
@@ -XX,6 +XX,20 @@ class CommunicationSimulator(...):
     def sender(self, message):
         sender_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM, 0)
         time.sleep(1)
-        sender_socket.connect((self._servername, self.observer_port))
+        # Suporte multi-container:
+        # - OMNeT++ pode estar em outro container/host.
+        # - Se OMNET_HOST/OMNET_PORT existirem, eles têm prioridade.
+        servername = os.getenv("OMNET_HOST", self._servername)
+        try:
+            observer_port = int(os.getenv("OMNET_PORT", str(self.observer_port)))
+        except ValueError:
+            observer_port = self.observer_port
+
+        sender_socket.connect((servername, observer_port))
         sender_socket.send(message)
         sender_socket.close()
